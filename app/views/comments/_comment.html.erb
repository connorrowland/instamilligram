<div class="post-comments">
	<h4><%= comment.user.username %></h4>

	<p><%= comment.content %></p>
	<% if comment.user == current_user %>
	<%= link_to 'Delete', post_comment_path(post, comment), method: :delete, remote: true, class:'thin-weight delete-comment' %>
	<% end %>
</div>
